namespace Y2021
{
    public class Day11
    {
        public int[,] OctopusEnergies { get; set; } = new int[10, 10];

        public int ComputeFlashes(int steps)
        {
            int countFlash = 0;

            //Each Step every energyLevel increases by 1
            //for (int i = 0; i < length; i++)
            //{

            //}

            //energylevel exceeds 9, increment flashcount, reset energylevel to 0
            //if (energyLevel > 9)
            //{
            //    countFlash++;
            //    energyLevel = 0;
            //}

            return countFlash;
        }
    }
}